import { fork } from 'redux-saga/effects';

export default function* rootSaga() {
    /*
     * Scanner
     * */
    // yield fork(ScannerSaga.fetchVerifyTicketAfterRequestSaga);

    /*
     * Ticket
     * */

    /*
     * Events
     * */
    
     // yield fork(EventsSaga.getConfirmResponseOnMessageEvent);
}
